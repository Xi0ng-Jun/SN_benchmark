(function () {
  'use strict';
  const C = window.SNExplorerCore;
  const $ = (id) => document.getElementById(id);
  const raw = JSON.parse(($('dashboard-data') || {}).textContent || '{}');
  const entries = Array.isArray(raw.entries) ? raw.entries : Object.values(raw.entries || {});
  const observations = raw.observations || {};
  const runs = raw.runs || [];
  const graph = raw.graph || {nodes: [], edges: []};
  const details = new Map();
  const pendingDetails = new Map();
  let detailToken = 0;
  let toastTimer;
  const STAGES = C.STEPS || ['source', 'normalize', 'partition', 'import', 'retrieve', 'synthesize', 'answer', 'score'];
  const stageNames = {source: '资料来源', normalize: '归一化', partition: '分区', import: '导入', retrieve: '检索', synthesize: '合成', answer: '回答', score: '评分'};
  const state = {view: 'map', run: null, case: null, step: 'source', span: null, query: '', filters: {}, detail: null, selectedNode: null, inspect: 'input', playing: false, saved: []};
  const esc = (value) => value == null ? '' : String(value);
  const fmt = (value) => typeof value === 'number' && Number.isFinite(value) ? value.toFixed(3).replace(/0+$/, '').replace(/\.$/, '') : value == null ? '—' : esc(value);
  const formatJson = (value) => { try { return JSON.stringify(value == null ? null : value, null, 2); } catch (_) { return esc(value); } };
  const node = (tag, className, content) => { const element = document.createElement(tag); if (className) element.className = className; if (content != null) element.textContent = content; return element; };
  const empty = (host, message) => host.replaceChildren(node('p', 'empty', message));
  function showToast(message) { const host = $('toast'); host.textContent = message; host.classList.add('show'); clearTimeout(toastTimer); toastTimer = setTimeout(() => host.classList.remove('show'), 2200); }
  const runId = (run) => run && (run.key || run.run_key || run.id);
  const obsArray = () => Object.entries(observations).map(([id, value]) => ({id, ...value}));
  const selectedObservation = () => observations[state.case] || obsArray().find((item) => item.case_id === state.case) || null;
  const entriesForRun = (run) => entries.filter((entry) => !run || entry.run_key === run);
  function casesForRun(run) { const ids = new Set(entriesForRun(run).map((entry) => entry.observation_id).filter(Boolean)); return obsArray().filter((item) => !run || item.run_key === run || ids.has(item.id)); }
  const setHash = () => history.replaceState(null, '', C.formatHash({view: state.view, run: state.run, case: state.case, step: state.step, span: state.span}));
  function setView(view) { state.view = C.VIEWS.includes(view) ? view : 'map'; document.querySelectorAll('[data-view]').forEach((button) => button.setAttribute('aria-selected', String(button.dataset.view === state.view))); document.querySelectorAll('[data-panel]').forEach((panel) => { panel.hidden = panel.dataset.panel !== state.view; }); setHash(); }
  function setSelection(run, caseId) { state.run = run || state.run || (runs[0] && runId(runs[0])) || null; const available = casesForRun(state.run); const match = caseId && available.find((item) => item.id === caseId || item.case_id === caseId); state.case = match ? match.id : (available[0] && available[0].id) || null; state.detail = state.case && details.get(state.case) || null; state.selectedNode = null; state.span = null; state.step = 'source'; setHash(); renderRail(); renderReplay(); }
  function stat(name, value) { const wrap = node('div', 'rail-stat'); wrap.append(node('strong', '', String(value)), node('span', '', name)); return wrap; }
  function renderRail() { $('suite-title').textContent = raw.summary && raw.summary.suite || (runs[0] && runs[0].suite) || '实验记录'; $('suite-meta').textContent = `${runs.length} 个 run · ${entries.length} 条评分条目`; const select = $('run-select'); select.replaceChildren(); runs.forEach((run) => { const option = document.createElement('option'); option.value = runId(run); option.textContent = `${run.mode || 'run'} · ${run.run_id || run.key || runId(run)}`; select.append(option); }); select.value = state.run || ''; const runRows = entriesForRun(state.run); const scored = runRows.filter(C.isValidScore).length; $('rail-stats').replaceChildren(stat('题目', casesForRun(state.run).length), stat('评分', runRows.length), stat('有效', scored)); const list = $('case-list'); list.replaceChildren(); const needle = ($('case-search').value || '').trim().toLocaleLowerCase(); const cases = casesForRun(state.run).filter((item) => !needle || `${item.case_id || ''} ${formatJson(item.case || item.question || '')}`.toLocaleLowerCase().includes(needle)); $('case-count').textContent = cases.length; cases.forEach((item) => { const button = node('button', `case-item ${item.id === state.case ? 'selected' : ''}`); button.type = 'button'; button.append(node('strong', '', item.case_id || item.id), node('span', '', `${entries.filter((entry) => entry.observation_id === item.id).length} 条评分 · ${item.output_status || '未记录'}`)); button.addEventListener('click', () => { setSelection(state.run, item.id); setView('replay'); }); list.append(button); }); $('current-run-label').textContent = state.run ? (runs.find((run) => runId(run) === state.run) || {}).run_id || state.run : '未选择 run'; }
  function renderMap() { const svg = $('experiment-map'); svg.replaceChildren(); const width = Math.max(640, svg.parentElement.clientWidth || 760), height = 510; svg.setAttribute('viewBox', `0 0 ${width} ${height}`); const nodes = graph.nodes || [], groups = {dataset: [], external: [], run: [], answers: [], scores: []}; nodes.forEach((item) => (groups[item.kind] || groups.run).push(item)); const order = ['dataset', 'external', 'run', 'answers', 'scores'], positions = new Map(); order.forEach((kind, column) => groups[kind].forEach((item, index) => positions.set(item.id, {x: 90 + column * ((width - 180) / 4), y: 90 + index * 92}))); (graph.edges || []).forEach((edge) => { const from = positions.get(edge.source), to = positions.get(edge.target); if (!from || !to) return; const line = document.createElementNS('http://www.w3.org/2000/svg', 'path'); line.setAttribute('d', `M ${from.x + 88} ${from.y} C ${(from.x + to.x) / 2} ${from.y}, ${(from.x + to.x) / 2} ${to.y}, ${to.x - 88} ${to.y}`); line.setAttribute('class', `edge ${edge.kind || ''}`); svg.append(line); }); nodes.forEach((item) => { const pos = positions.get(item.id); if (!pos) return; const group = document.createElementNS('http://www.w3.org/2000/svg', 'g'); group.setAttribute('class', `map-node ${item.kind} ${state.selectedNode === item.id ? 'selected' : ''}`); group.setAttribute('tabindex', '0'); group.setAttribute('role', 'button'); group.addEventListener('click', () => { state.selectedNode = item.id; if (item.run_key) setSelection(item.run_key, item.case_id); renderMapInspector(item); renderMap(); }); const rect = document.createElementNS('http://www.w3.org/2000/svg', 'rect'); rect.setAttribute('x', pos.x - 88); rect.setAttribute('y', pos.y - 30); rect.setAttribute('width', '176'); rect.setAttribute('height', '60'); rect.setAttribute('rx', '8'); group.append(rect); const title = document.createElementNS('http://www.w3.org/2000/svg', 'text'); title.setAttribute('x', pos.x - 72); title.setAttribute('y', pos.y - 5); title.textContent = item.label || item.id; group.append(title); const meta = document.createElementNS('http://www.w3.org/2000/svg', 'text'); meta.setAttribute('x', pos.x - 72); meta.setAttribute('y', pos.y + 15); meta.setAttribute('class', 'node-meta'); meta.textContent = `${item.status || 'recorded'} · ${item.counts ? formatJson(item.counts).replace(/[{}\"]/g, '').slice(0, 26) : ''}`; group.append(meta); svg.append(group); }); $('map-counts').replaceChildren(stat('来源', groups.dataset.length + groups.external.length), stat('运行', groups.run.length), stat('答卷', groups.answers.length), stat('评分', groups.scores.length)); if (!state.selectedNode && nodes[0]) renderMapInspector(nodes[0]); }
  function renderMapInspector(item) { const host = $('map-inspector'); host.replaceChildren(node('span', 'eyebrow', `${item.kind || '节点'} · ${item.status || 'recorded'}`), node('h3', '', item.label || item.id), node('p', 'inspector-copy', item.provenance || '身份来自离线索引；点击关联节点进入单题回放。')); const related = entries.filter((entry) => (item.run_key && entry.run_key === item.run_key) || (item.case_id && entry.case_id === item.case_id)); if (related.length) host.append(node('h4', '', `${related.length} 条相关评分`), ...related.slice(0, 5).map((entry) => { const b = node('button', 'link-row', `${entry.case_id} · ${entry.metric_name || entry.scorer} · ${fmt(entry.score)}`); b.type = 'button'; b.addEventListener('click', () => { setSelection(entry.run_key, entry.observation_id); setView('replay'); }); return b; })); }
  function detailPath(observation) { return observation && (observation.detail_file || (observation.detail && observation.detail.file)); }
  function loadDetail(observation) { if (!observation) return Promise.resolve(null); const id = observation.id; if (details.has(id)) return Promise.resolve(details.get(id)); if (pendingDetails.has(id)) return pendingDetails.get(id); const path = detailPath(observation); if (!path) return Promise.resolve(null); const token = ++detailToken; const pending = new Promise((resolve) => { let done = false; const finish = (payload) => { if (done) return; done = true; pendingDetails.delete(id); if (payload) details.set(id, payload); if (token === detailToken && state.case === id) { state.detail = payload; if (state.span) { const flat = C.flattenTraceTree(C.selectDisplayTraces(payload && payload.native && payload.native.traces || [])); state.selectedNode = flat.find((item) => item.id === state.span) || null; } renderReplay(); if (state.selectedNode) renderInspector(state.selectedNode); } resolve(payload || null); }; pendingDetails.set(id, {finish}); const script = document.createElement('script'); script.src = path; script.async = true; script.onload = () => setTimeout(() => finish(details.get(id) || null), 0); script.onerror = () => finish(null); document.head.append(script); }); return pending; }
  function renderReplay() { const observation = selectedObservation(), title = observation ? observation.case_id || observation.id : '选择一题开始回放'; $('replay-title').textContent = title; $('replay-kicker').textContent = observation ? `${observation.mode || '实验'} · ${observation.output_status || '未记录'}` : '未选择'; $('replay-subtitle').textContent = observation ? (observation.question || '完整题面在详情工件中按需加载。') : '步骤选择只改变当前焦点；原生时间和父子身份来自真实 trace。'; const detail = state.detail, steps = detail && Array.isArray(detail.steps) ? detail.steps : STAGES.map((id) => ({id, title: stageNames[id], status: 'missing', description: '尚未加载逐题详情。'})); const flow = $('stage-flow'); flow.replaceChildren(); steps.forEach((step) => { const button = node('button', `stage ${step.id === state.step ? 'active' : ''} ${step.status || 'missing'}`); button.type = 'button'; button.setAttribute('role', 'listitem'); button.append(node('span', 'stage-index', String(STAGES.indexOf(step.id) + 1).padStart(2, '0')), node('strong', '', step.title || stageNames[step.id] || step.id), node('small', '', step.status || 'missing')); button.addEventListener('click', () => { state.step = step.id; setHash(); renderReplay(); }); flow.append(button); }); const current = steps.find((step) => step.id === state.step) || steps[0]; if (!current) return; $('step-title').textContent = current.title || stageNames[current.id] || current.id; $('step-status').textContent = current.status || 'missing'; $('step-status').className = `status-badge ${current.status || 'missing'}`; const detailHost = $('step-detail'); detailHost.replaceChildren(node('p', '', current.description || '')); [['输入', current.input], ['输出', current.output], ['字段说明', current.fields], ['产物', current.artifacts], ['代码', current.code]].forEach(([name, value]) => { if (value == null) return; const block = node('section', 'evidence-block'); block.append(node('h4', '', name), node('pre', '', formatJson(value))); detailHost.append(block); }); const traces = C.selectDisplayTraces(detail && detail.native && detail.native.traces || []), flat = C.flattenTraceTree(traces); $('trace-count').textContent = flat.length ? `${flat.length} 个 span` : '未采集'; const tree = $('trace-tree'); tree.replaceChildren(); flat.forEach((item) => { const button = node('button', `trace-row ${state.span === item.id ? 'selected' : ''}`); button.type = 'button'; button.style.paddingLeft = `${12 + item.depth * 18}px`; button.append(node('span', 'trace-mark', item.depth ? '↳' : '●'), node('strong', '', item.span.name || item.id), node('small', '', item.span.duration_ms != null ? `${item.span.duration_ms} ms` : (item.request_id || ''))); button.addEventListener('click', () => { state.span = item.id; state.selectedNode = item; state.inspect = 'input'; setHash(); renderReplay(); renderInspector(item); }); tree.append(button); }); if (state.selectedNode && state.selectedNode.span) renderInspector(state.selectedNode); if (!detail && observation) loadDetail(observation); }
  function renderInspector(item) { $('inspector-title').textContent = item && item.span ? item.span.name || item.id : '选择一个节点'; const host = $('inspector-content'); host.replaceChildren(); if (!item) { empty(host, '选择 trace span 查看精确输入、输出和分数。'); return; } const span = item.span || {}, related = C.entriesForSpan((state.detail && state.detail.entries) || entries, {span_id: span.span_id || span.uuid || span.id, sample_id: span.sample_id, request_id: item.request_id}); const value = state.inspect === 'input' ? span.input || span.attributes || {} : state.inspect === 'output' ? span.output || span.result || {} : state.inspect === 'schema' ? span.fields || span.schema || {} : {artifacts: span.artifacts || [], code: span.code || []}; host.append(node('pre', '', formatJson(value))); if (related.length) { host.append(node('h4', '', '关联指标')); related.forEach((entry) => { const row = node('div', 'metric-row'); row.append(node('strong', '', entry.metric_name || entry.scorer), node('span', '', `${entry.status || 'missing'} · ${fmt(entry.score)}`)); host.append(row); }); } }
  function renderFacets() { const host = $('facet-bar'); host.replaceChildren(); C.FACETS.forEach((field) => { const values = C.facetOptions(entriesForRun(state.run), field, state.filters, state.query); if (!values.length) return; const detailsEl = document.createElement('details'); detailsEl.className = 'facet'; const summary = document.createElement('summary'); summary.textContent = field; detailsEl.append(summary); values.slice(0, 12).forEach(({value, count}) => { const labelEl = node('label', 'facet-option'); const checkbox = document.createElement('input'); checkbox.type = 'checkbox'; checkbox.checked = (state.filters[field] || []).includes(value); checkbox.addEventListener('change', () => { const current = new Set(state.filters[field] || []); checkbox.checked ? current.add(value) : current.delete(value); state.filters[field] = [...current]; if (!state.filters[field].length) delete state.filters[field]; renderAnalysis(); }); labelEl.append(checkbox, node('span', '', value), node('small', '', String(count))); detailsEl.append(labelEl); }); host.append(detailsEl); }); }
  function renderFacets() { const host = $('facet-bar'); host.replaceChildren(); C.FACETS.forEach((field) => { const values = C.facetOptions(state.view === 'analysis' ? entries : entriesForRun(state.run), field, state.filters, state.query); if (!values.length) return; const detailsEl = document.createElement('details'); detailsEl.className = 'facet'; const summary = document.createElement('summary'); summary.textContent = field; detailsEl.append(summary); values.forEach(({value, count}) => { const labelEl = node('label', 'facet-option'); const checkbox = document.createElement('input'); checkbox.type = 'checkbox'; checkbox.checked = (state.filters[field] || []).includes(value); checkbox.addEventListener('change', () => { const selected = new Set(state.filters[field] || []); checkbox.checked ? selected.add(value) : selected.delete(value); state.filters[field] = [...selected]; if (!state.filters[field].length) delete state.filters[field]; renderAnalysis(); }); labelEl.append(checkbox, node('span', '', value), node('small', '', String(count))); detailsEl.append(labelEl); }); host.append(detailsEl); }); }
  const currentRows = () => C.filterEntries(state.view === 'analysis' ? entries : entriesForRun(state.run), state.filters, state.query);
  function renderAnalysis() { const rows = currentRows(); renderFacets(); $('active-filters').replaceChildren(...Object.entries(state.filters).flatMap(([field, values]) => values.map((value) => { const chip = node('button', 'chip', `${field}: ${value} ×`); chip.type = 'button'; chip.addEventListener('click', () => { state.filters[field] = state.filters[field].filter((item) => item !== value); if (!state.filters[field].length) delete state.filters[field]; renderAnalysis(); }); return chip; }))); const summary = C.summarizeEntries(rows); $('analysis-summary').replaceChildren(stat('答卷', summary.observations), stat('评分条目', summary.scoringEntries), stat('有效分数', summary.validScores), stat('均值', fmt(summary.mean))); const groups = new Map(); rows.forEach((entry) => { const key = `${entry.mode || '未记录'}\u0000${entry.metric_name || entry.scorer || '未记录'}\u0000${entry.config_family || entry.run_key}`; if (!groups.has(key)) groups.set(key, []); groups.get(key).push(entry); }); const groupHost = $('group-results'); groupHost.replaceChildren(); [...groups.values()].forEach((group) => { const valid = group.filter(C.isValidScore), article = node('article', 'result-group'); article.append(node('strong', '', `${group[0].mode || '未记录'} · ${group[0].metric_name || group[0].scorer}`), node('span', '', `${valid.length}/${group.length} 有效 · ${valid.length ? fmt(valid.reduce((sum, entry) => sum + entry.score, 0) / valid.length) : '—'}`)); const meter = node('i', 'coverage-meter'); meter.style.width = `${group.length ? valid.length / group.length * 100 : 0}%`; article.append(meter); groupHost.append(article); }); const body = $('analysis-rows'); body.replaceChildren(); rows.slice(0, 200).forEach((entry) => { const tr = document.createElement('tr'); tr.append(cell(entry.case_id || entry.observation_id), cell(`${entry.mode || '—'} · ${entry.metric_name || entry.scorer}`), cell(entry.output_status || 'missing'), cell(entry.status || 'missing'), cell(fmt(entry.score))); const action = document.createElement('td'), button = node('button', 'text-button', '回放'); button.type = 'button'; button.addEventListener('click', () => { setSelection(entry.run_key, entry.observation_id); setView('replay'); }); action.append(button); tr.append(action); body.append(tr); }); $('analysis-count').textContent = `${rows.length} 条 · 显示前 ${Math.min(rows.length, 200)} 条`; renderSaved(); enhanceAnalysis(); }
  const cell = (value) => node('td', '', value);
  function renderSaved() { const host = $('saved-groups'); host.replaceChildren(); state.saved.forEach((group, index) => { const card = node('article', 'saved-card'), head = node('div', 'saved-head'); head.append(node('strong', '', `${index === 0 ? '参照 · ' : ''}${group.name}`)); const remove = node('button', 'icon-button', '×'); remove.type = 'button'; remove.addEventListener('click', () => { state.saved.splice(index, 1); renderSaved(); }); head.append(remove); card.append(head, node('p', '', `${group.rows.length} 条评分 · ${group.query || '无搜索'}`)); host.append(card); }); if (state.saved.length >= 2) { const result = C.pairEntries(state.saved[0].rows, state.saved[1].rows), card = node('article', `compare-card ${result.ok ? '' : 'warning'}`); if (!result.ok) card.append(node('strong', '', `无法逐题比较：${result.code}`), result.distribution ? node('p', '', `组件分布 ${result.distribution.reference} / ${result.distribution.comparison}，保留为分布。`) : null); else { const valid = result.pairs.filter((pair) => pair.delta != null), delta = valid.length ? valid.reduce((sum, pair) => sum + pair.delta, 0) / valid.length : null; card.append(node('strong', '', '同题配对'), node('p', '', `${result.pairs.length} 个共同 case · ${valid.length} 个共同有效 · 均值差 ${fmt(delta)}`)); result.pairs.slice(0, 8).forEach((pair) => { const row = node('button', 'pair-row', `${pair.case_id} · ${fmt(pair.reference.score)} → ${fmt(pair.comparison.score)} · ${pair.state}`); row.type = 'button'; row.addEventListener('click', () => { setSelection(pair.reference.run_key, pair.reference.observation_id); setView('replay'); }); card.append(row); }); } host.append(card); } }
  function saveGroup() { const name = window.prompt('比较组名称', `比较组 ${state.saved.length + 1}`); if (name == null) return; state.saved.push({name: name.trim() || `比较组 ${state.saved.length + 1}`, rows: currentRows(), filters: JSON.parse(JSON.stringify(state.filters)), query: state.query}); renderSaved(); renderConfigComparison(); }
  function enhanceAnalysis() { const host = $('group-results'); if (!host) return; const rows = currentRows().filter(C.isValidScore); const chart = node('div', 'distribution-chart'); chart.append(node('h4', '', '有效分数分布')); const bins = Array.from({length: 10}, () => 0); rows.forEach((entry) => bins[Math.max(0, Math.min(9, Math.floor(entry.score * 10))) ] += 1); const max = Math.max(1, ...bins); const bars = node('div', 'distribution-bars'); bins.forEach((count, index) => { const bar = node('i', 'distribution-bar'); bar.style.height = `${Math.max(3, count / max * 64)}px`; bar.title = `${(index / 10).toFixed(1)}–${((index + 1) / 10).toFixed(1)}: ${count}`; bars.append(bar); }); chart.append(bars); host.prepend(chart); }
  function renderConfigComparison() { const host = $('saved-groups'); if (!host || state.saved.length < 2) return; const prior = host.querySelector('.config-diff'); if (prior) prior.remove(); const runFor = (rows) => { const key = rows.find((entry) => entry.run_key || entry.run_id); return runs.find((run) => runId(run) === (key && (key.run_key || key.run_id))); }; const summaryFor = (run) => run && (run.config_summary || (run.manifest && (run.manifest.config_summary || run.manifest.identity))) || null; const left = summaryFor(runFor(state.saved[0].rows)), right = summaryFor(runFor(state.saved[1].rows)); const card = node('article', 'config-diff'); card.append(node('strong', '', '配置身份')); if (!left || !right) { card.append(node('p', '', '配置摘要未保存，无法判断两组设置差异。')); host.append(card); return; } const diff = C.diffConfig(left, right); card.append(node('p', '', diff.length ? `${diff.length} 个字段不同` : '两组配置摘要一致')); diff.slice(0, 12).forEach((item) => { const row = node('div', 'config-diff-row'); row.append(node('code', '', item.path), node('span', '', `${fmt(item.reference)} → ${fmt(item.comparison)}`)); card.append(row); }); host.append(card); }
  function render() { renderRail(); const activeRun = runs.find((run) => runId(run) === state.run) || {}; const demo = activeRun.demo === true || (activeRun.manifest && activeRun.manifest.demo === true) || (activeRun.source && activeRun.source.synthetic === true) || (activeRun.manifest && activeRun.manifest.identity && activeRun.manifest.identity.source && activeRun.manifest.identity.source.synthetic === true); if (demo) $('suite-meta').textContent += ' · 构造演示数据'; renderMap(); renderReplay(); renderAnalysis(); enhanceAnalysis(); renderConfigComparison(); setView(state.view); }
  $('run-select').addEventListener('change', (event) => setSelection(event.target.value, null)); $('case-search').addEventListener('input', renderRail); $('analysis-search').addEventListener('input', (event) => { state.query = event.target.value; renderAnalysis(); }); $('clear-filters').addEventListener('click', () => { state.filters = {}; state.query = ''; $('analysis-search').value = ''; renderAnalysis(); }); $('save-group-button').addEventListener('click', saveGroup); document.querySelectorAll('[data-view]').forEach((button) => button.addEventListener('click', () => setView(button.dataset.view))); $('share-button').addEventListener('click', async () => { const url = location.href; try { await navigator.clipboard.writeText(url); showToast('当前链接已复制'); } catch (_) { showToast(url); } }); $('export-button').addEventListener('click', () => { const blob = new Blob([JSON.stringify({format: 'sn-explorer-selection-v1', filters: state.filters, query: state.query, entries: currentRows()}, null, 2)], {type: 'application/json'}); const link = document.createElement('a'); link.href = URL.createObjectURL(blob); link.download = 'experiment-selection.json'; link.click(); setTimeout(() => URL.revokeObjectURL(link.href), 0); }); $('prev-step').addEventListener('click', () => moveStep(-1)); $('next-step').addEventListener('click', () => moveStep(1)); $('reset-step').addEventListener('click', () => { state.step = 'source'; setHash(); renderReplay(); }); $('play-step').addEventListener('click', () => { state.playing = !state.playing; $('play-step').textContent = state.playing ? 'Ⅱ' : '▶'; if (state.playing) tick(); }); document.querySelectorAll('[data-inspect]').forEach((button) => button.addEventListener('click', () => { state.inspect = button.dataset.inspect; document.querySelectorAll('[data-inspect]').forEach((item) => item.setAttribute('aria-selected', String(item === button))); renderInspector(state.selectedNode); }));
  function moveStep(delta) { const index = Math.max(0, Math.min(STAGES.length - 1, STAGES.indexOf(state.step) + delta)); state.step = STAGES[index]; setHash(); renderReplay(); }
  function tick() { if (!state.playing) return; moveStep(1); if (state.step === 'score') { state.playing = false; $('play-step').textContent = '▶'; return; } setTimeout(tick, 720); }
  window.SNExplorer.registerDetail = (id, payload) => { const pending = pendingDetails.get(id); if (pending) pending.finish(payload); else details.set(id, payload); };
  document.addEventListener('keydown', (event) => { if (event.target && ['INPUT', 'TEXTAREA', 'SELECT'].includes(event.target.tagName)) return; if (event.key === 'ArrowLeft' && state.view === 'replay') { event.preventDefault(); moveStep(-1); } else if (event.key === 'ArrowRight' && state.view === 'replay') { event.preventDefault(); moveStep(1); } else if (event.key === ' ' && state.view === 'replay') { event.preventDefault(); $('play-step').click(); } else if (event.key === '1') setView('map'); else if (event.key === '2') setView('replay'); else if (event.key === '3') setView('analysis'); });
  window.addEventListener('hashchange', () => { const normalized = C.normalizeHashState(C.parseHash(location.hash), raw, state.detail); Object.assign(state, normalized.state); if (normalized.invalid.length) showToast(`已回到可用选择：${normalized.invalid.join('、')}`); render(); });
  const initial = C.normalizeHashState(C.parseHash(location.hash), raw); Object.assign(state, initial.state); if (!state.run && runs[0]) state.run = runId(runs[0]); if (!state.case) { const first = casesForRun(state.run)[0]; state.case = first && first.id; } render();
})();
